const ExpenseFilter = () => {
  return (
    <div>
      <select className="form-select">
        <option value="">All categories</option>
        <option value="">All categories</option>
        <option value="">All categories</option>
        <option value="">All categories</option>
      </select>
    </div>
  );
};

export default ExpenseFilter;
